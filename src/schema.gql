# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String!
  refreshToken: String!
}

input CreateNoteAttachment {
  mime: String!
  name: String!
  noteId: String!
  size: Int!
  url: String!
}

input CreateNoteInput {
  content: String
  labels: [String!]
  pinned: Boolean = false
  postion: Float
  projectId: String!
  reminderAt: DateTime
  title: String!
  visibility: String
}

input CreateProjectInput {
  description: String
  name: String!
}

input CreateUserInput {
  email: String!
  name: String!
  password: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Mutation {
  addAttachment(input: CreateNoteAttachment!): Note!
  archiveProject(projectId: String!): Project!
  createNote(input: CreateNoteInput!): Note!
  createProject(input: CreateProjectInput!): Project!
  logout: Boolean!
  refreshTokens: AuthResponse!
  removeAttachment(attachmentId: String!, noteId: String!): Note!
  removeNote(noteId: String!): Boolean!
  removeProject(projectId: String!): Boolean!
  signIn(input: SignInInput!): AuthResponse!
  signUp(input: CreateUserInput!): AuthResponse!
  unarchiveProject(projectId: String!): Project!
  updateProject(input: UpdateProjectInput!, projectId: String!): Project!
}

type Note {
  attachments: [NoteAttachment]
  content: String
  createdAt: DateTime
  id: ID!
  isArchived: Boolean!
  labels: [String!]!
  ownerId: String!
  pinned: Boolean
  position: Float!
  projectId: String!
  reminderAt: DateTime
  reminderSent: Boolean!
  title: String!
  trashedAt: DateTime
  updatedAt: DateTime
  visibility: NoteVisibility!
}

type NoteAttachment {
  id: String!
  mime: String!
  name: String!
  size: Float!
  url: String!
}

"""The visibility status of the note"""
enum NoteVisibility {
  PRIVATE
  PUBLIC
  SHARED
}

type Project {
  createdDate: DateTime!
  description: String
  id: ID!
  isArchive: Boolean!
  modifiedDate: DateTime!
  name: String!
  noteCount: Float!
  notes: [Note]
  ownerName: String!
}

type Query {
  getProject(projectId: String!): Project!
  getProjectsNotes(projectId: String!): [Note!]!
  getUsersNotes: [Note!]!
  getUsersProjects: [Project!]!
  hello: String!
  search(query: String!): [SearchResult!]!
}

union SearchResult = Note | Project

input SignInInput {
  email: String!
  password: String!
}

input UpdateProjectInput {
  description: String
  name: String
}