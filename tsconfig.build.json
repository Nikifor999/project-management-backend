{
  "extends": "./tsconfig.json",
  "exclude": [
    "node_modules",
    "test",
    "dist",
    "**/*spec.ts",
    "vitest.config.ts",
    "src/**/*.spec.ts"
  ],
  "compilerOptions": {
    // Мы явно говорим: при сборке нужны только типы Node.js, без Vitest.
    "types": [
      "node"
    ]
  }
}